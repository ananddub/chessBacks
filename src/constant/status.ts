export enum Status {
    PENDING = 'pending',
    ACCEPTED = 'accepted',
    REJECTED = 'rejected',
    PLAYING = 'playing',
    CANCELLED = 'cancelled',
    LOBBY = 'lobby',
    FINISHED = 'finished',
}
