apiVersion: v1
kind: Service
metadata:
  name: chess-backend
  namespace: chess
spec:
  selector:
    app: chess-backend-deployment
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000

